import { Component,inject } from '@angular/core';import { HttpClient } from '@angular/common/http';import { FormBuilder,ReactiveFormsModule,Validators } from '@angular/forms';import { RouterLink } from '@angular/router';
@Component({standalone:true,imports:[ReactiveFormsModule,RouterLink],template:`<section class="container"><div class="card box"><h1>Reset password</h1><p class="muted">Enter your email. The development backend currently returns the reset token in the response; production must send it only through notification service.</p><form class="form" [formGroup]="form" (ngSubmit)="submit()"><div class="field"><label>Email</label><input type="email" formControlName="email"></div><button class="btn btn-primary" [disabled]="form.invalid">Request reset</button></form>@if(result){<p class="success">Request accepted. Development token: <code>{{result}}</code></p>}<a routerLink="/login">Back</a></div></section>`,styles:[`.box{max-width:560px;margin:50px auto}`]})
export class ForgotPasswordComponent{private fb=inject(FormBuilder);private http=inject(HttpClient);result='';form=this.fb.nonNullable.group({email:['',[Validators.required,Validators.email]]});submit(){this.http.post<{devResetToken:string}>('/api/auth/password-reset/request',this.form.getRawValue()).subscribe(r=>this.result=r.devResetToken)}}
