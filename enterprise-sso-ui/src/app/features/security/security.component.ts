import { Component,inject } from '@angular/core';import { FormBuilder,ReactiveFormsModule,Validators } from '@angular/forms';import { HttpClient } from '@angular/common/http';import { AuthService } from '../../core/services/auth.service';import { MfaSetupResponse } from '../../core/models/auth.models';
@Component({standalone:true,imports:[ReactiveFormsModule],template:`<section class="container"><h1 class="page-title">Security</h1><div class="grid grid-3"><article class="card"><h2>Multi-factor authentication</h2><p class="muted">Configure TOTP or email MFA.</p><form class="form" [formGroup]="form" (ngSubmit)="setup()"><div class="field"><label>Method</label><select formControlName="type"><option value="TOTP">Authenticator app (TOTP)</option><option value="EMAIL">Email OTP</option><option value="NONE">Disable</option></select></div><button class="btn btn-primary">Save MFA method</button></form>@if(result){<p class="success">{{result.message}}</p>@if(result.secret){<div class="secret"><strong>Secret</strong><code>{{result.secret}}</code><small>Store this once in an authenticator app.</small></div>}}</article><article class="card"><h2>Active session</h2><p>Access tokens are held in <strong>sessionStorage</strong>, so closing the tab clears them.</p><p class="muted">For a higher-security production architecture, use a BFF and Secure, HttpOnly, SameSite cookies instead.</p></article><article class="card"><h2>Current limitation</h2><p class="error">The backend has no endpoint to verify an OTP challenge. Enabling MFA today does not complete a secure second-factor login.</p></article></div></section>`,styles:[`.secret{display:grid;gap:8px;margin-top:15px}.secret code{word-break:break-all;background:#f1f5f9;padding:10px}`]})
export class SecurityComponent{private fb=inject(FormBuilder);private http=inject(HttpClient);auth=inject(AuthService);result?:MfaSetupResponse;form=this.fb.nonNullable.group({type:['TOTP',[Validators.required]]});setup(){const email=this.auth.currentUser?.email;if(!email)return;this.http.post<MfaSetupResponse>(`/api/auth/mfa/${encodeURIComponent(email)}`,this.form.getRawValue()).subscribe(r=>this.result=r)}}
