import { Component,inject } from '@angular/core';import { HttpClient } from '@angular/common/http';import { FormBuilder,ReactiveFormsModule,Validators } from '@angular/forms';
@Component({standalone:true,imports:[ReactiveFormsModule],template:`<section class="container"><h1 class="page-title">Identity administration</h1><p class="muted">Assign RBAC roles. This page and API require ADMIN or SUPER_ADMIN.</p><div class="card admin"><form class="form" [formGroup]="form" (ngSubmit)="save()"><div class="field"><label>User email</label><input type="email" formControlName="email"></div><fieldset><legend>Roles</legend>@for(role of roles;track role){<label class="role"><input type="checkbox" [checked]="selected.has(role)" (change)="toggle(role)"> {{role}}</label>}</fieldset><button class="btn btn-primary" [disabled]="form.invalid||selected.size===0">Update roles</button></form>@if(message){<p class="success">{{message}}</p>}</div></section>`,styles:[`.admin{max-width:650px}.role{display:inline-flex;margin:10px 18px 10px 0}`]})
export class AdminComponent{private fb=inject(FormBuilder);private http=inject(HttpClient);roles=['USER','ADMIN','SUPER_ADMIN','AUDITOR','SUPPORT'];selected=new Set<string>(['USER']);message='';form=this.fb.nonNullable.group({email:['',[Validators.required,Validators.email]]});toggle(r:string){this.selected.has(r)?this.selected.delete(r):this.selected.add(r)}save(){this.http.post<void>('/api/auth/admin/roles',{email:this.form.getRawValue().email,roles:[...this.selected]}).subscribe(()=>this.message='Roles updated successfully.')}}
