import { Component,inject } from '@angular/core';import { JsonPipe } from '@angular/common';import { UserService } from '../../core/services/user.service';
@Component({standalone:true,imports:[JsonPipe],template:`<section class="container"><h1 class="page-title">My profile</h1><p class="muted">Profile data is loaded through API Gateway → user-service → auth-service.</p>@if(profile){<div class="card"><dl><dt>Username</dt><dd>{{profile.username||profile.displayName||'—'}}</dd><dt>Email</dt><dd>{{profile.email||'—'}}</dd><dt>User ID</dt><dd>{{profile.userId||'—'}}</dd></dl><details><summary>Raw service response</summary><pre>{{profile|json}}</pre></details></div>}@else{<div class="card">Loading profile…</div>}</section>`,styles:[`dl{display:grid;grid-template-columns:150px 1fr;gap:14px}dt{font-weight:800}dd{margin:0}pre{overflow:auto;background:#f8fafc;padding:14px}`]})
export class ProfileComponent{private users=inject(UserService);profile:any;constructor(){this.users.me().subscribe({next:p=>this.profile=p,error:()=>this.profile={}})}}
